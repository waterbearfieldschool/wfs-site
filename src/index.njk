---
layout: layouts/home.njk
title: "Waterbear Field School"
description: "Outdoor Resilience Skill-Share for Kids and Adults"
image: /assets/images/biketrailer/full_system.webp
---

<!-- Hero Section -->
<section style="text-align: center; margin: 0 0 2rem; padding: 1.5rem; background: var(--card); border-radius: 16px; box-shadow: var(--shadow);">
  <h2 style="color: var(--accent); margin: 0 0 0.5rem; font-size: 1.6rem;">Learn Practical Resilience Skills</h2>
  <p style="color: var(--muted); margin: 0; max-width: 600px; margin: 0 auto;">Hands-on workshops teaching energy, water, food, shelter, climate, communication, fabrication, transport, arts & crafts.</p>
</section>

<div class="slideshow-container">
  <div class="slideshow">
    <div class="slide active">
      <img src="/assets/images/main/wfs1.jpg" alt="Solar Workshop" />
    </div>
    <div class="slide">
      <img src="/assets/images/solar/solar_workshop_ad.jpeg" alt="Sail Tracker" />
    </div>
    <div class="slide">
      <img src="/assets/images/main/wfs3.jpg" alt="Solar Workshop" />
    </div>
    <div class="slide">
      <img src="/assets/images/main/wfs6.jpg" alt="Solar Workshop" />
    </div>
    <div class="slide">
      <img src="/assets/images/main/wfs7.jpg" alt="Solar Workshop" />
    </div>
    <div class="slide">
      <img src="/assets/images/main/wfs8.jpg" alt="Solar Workshop" />
    </div>
    <div class="slide">
      <img src="/assets/images/main/wfs9.jpg" alt="Solar Workshop" />
    </div>
    <div class="slide">
      <img src="/assets/images/main/wfs11.jpg" alt="Solar Workshop" />
    </div>
    <div class="slide">
      <img src="/assets/images/projects/solar_testing.jpg" alt="Solar Testing" />
    </div>
    <div class="slide">
      <img src="/assets/images/main/wfs2.jpg" alt="Solar Workshop" />
    </div>
    <div class="slide">
      <img src="/assets/images/projects/solar101.jpg" alt="Solar Workshop" />
    </div>
    <div class="slide">
      <img src="/assets/images/main/tent.png" alt="Waterbear Field School" />
    </div>
  </div>
  <div class="slideshow-dots">
    <span class="dot active" onclick="currentSlide(1)"></span>
    <span class="dot" onclick="currentSlide(2)"></span>
    <span class="dot" onclick="currentSlide(3)"></span>
    <span class="dot" onclick="currentSlide(4)"></span>
    <span class="dot" onclick="currentSlide(5)"></span>
    <span class="dot" onclick="currentSlide(6)"></span>
    <span class="dot" onclick="currentSlide(7)"></span>
    <span class="dot" onclick="currentSlide(8)"></span>
    <span class="dot" onclick="currentSlide(9)"></span>
    <span class="dot" onclick="currentSlide(10)"></span>
    <span class="dot" onclick="currentSlide(11)"></span>
    <span class="dot" onclick="currentSlide(12)"></span>
  </div>
</div>

<!-- Foundations + Winter 2026 Sequence -->
<section style="margin-bottom: 2rem;" id="foundations">
  <div style="background: var(--card); border: 1px solid var(--hair); border-radius: 16px; padding: 1.5rem; box-shadow: var(--shadow);">
    <h2 style="color: var(--accent); font-size: 1.4rem; text-align: center; margin: 0 0 0.5rem;">The Foundations</h2>
    <p style="color: var(--muted); text-align: center; max-width: 600px; margin: 0 auto 1rem;">We're building a comprehensive curriculum for practical resilience - eight foundations covering the essential systems that sustain households and communities.</p>
    <div class="grid foundations-grid" style="gap: 10px;">
      {% for p in collections.allFoundations %}
      <a href="{{ p.url }}" class="foundation-card{% if foundationInfo[p.data.foundation].inWinter2026 %} highlighted{% endif %}" data-foundation="{{ p.data.foundation }}" style="display: block; background: var(--card); border: 1px solid var(--hair); border-top: 3px solid; border-radius: 12px; padding: 1rem; text-decoration: none; text-align: center; transition: transform 0.2s ease, box-shadow 0.2s ease;">
        <div style="font-size: 1.5rem; margin-bottom: 4px;">{{ foundationInfo[p.data.foundation].emoji }}</div>
        <div style="font-weight: 600; color: var(--fg); font-size: 0.9rem;">{{ p.data.title }}</div>
      </a>
      {% endfor %}
    </div>

    <div style="margin-top: 1.5rem; padding-top: 1.5rem; border-top: 1px solid var(--hair); text-align: center;">
      <h3 style="color: var(--accent); margin: 0 0 0.5rem; font-size: 1.4rem;">The Winter 2026 Foundations Sequence</h3>
      <p style="color: var(--muted); margin: 0 0 1rem; max-width: 500px; margin-left: auto; margin-right: auto;">A 4-week cohort program covering Energy, Climate, Communication, and Transport. Four workshops, four take-home projects.</p>
      <div style="display: flex; gap: 1rem; justify-content: center; flex-wrap: wrap; margin-bottom: 1rem;">
        <a href="/resilience-sequence/winter-2026/" style="display: inline-block; background: var(--accent); color: white; padding: 0.6rem 1.2rem; border-radius: 6px; text-decoration: none; font-weight: 600;">Register for the Winter 2026 Sequence</a>
      </div>
      <div class="grid" style="margin: 1rem 0;">
        {% for slug in ['off-grid-power', 'air-filtration', 'mesh-radio', 'biketrailer'] %}
        {% for p in collections.allWorkshops %}
        {% if p.fileSlug == slug %}
        <article class="card">
          {% if p.data.foundations %}{% for f in p.data.foundations %}{% if loop.first %}
          <div class="workshop-foundation-band" data-foundation="{{ f }}">
            {{ foundationInfo[f].emoji }} {{ foundationInfo[f].name }}
          </div>
          {% endif %}{% endfor %}{% endif %}
          <a href="{{ p.url }}" class="thumb" aria-label="{{ p.data.title }}">
            {% if p.data.image %}
              <img src="{{ p.data.image }}" alt="{{ p.data.title }}" />
            {% else %}
              [FIELD SCHOOL]
            {% endif %}
          </a>
          <div class="card-content">
            <h3 style="text-align: center;"><a href="{{ p.url }}">{{ p.data.title }}</a></h3>
            {% if p.data.meta %}<div class="meta-description">{{ p.data.meta }}</div>{% endif %}
          </div>
        </article>
        {% endif %}
        {% endfor %}
        {% endfor %}
      </div>
    </div>
  </div>
</section>

<section style="margin-bottom: 2rem;" id="current-workshops">
  <div style="background: var(--card); border: 1px solid var(--hair); border-radius: 16px; padding: 1.5rem; box-shadow: var(--shadow);">
    <h2 style="color: var(--accent); font-size: 1.4rem; text-align: center; margin: 0 0 0.5rem;">All Current Workshops</h2>
    <p style="color: var(--muted); text-align: center; max-width: 600px; margin: 0 auto 0.5rem;">Our workshops are typically held outdoors, in most weather. Pricing, format, and location vary by workshop and by teacher. Click on a workshop below to find out more!</p>
    <p style="color: var(--muted); text-align: center; max-width: 600px; margin: 0 auto 1rem;">Note: you can register for the workshops individually below — or take them as part of the <a href="/resilience-sequence/winter-2026/">Winter 2026 Sequence</a> above.</p>
    <div class="grid">
    {% for p in collections.allWorkshops %}{% if p.data.current %}
    <article class="card">
      {% if p.data.foundations %}{% for f in p.data.foundations %}{% if loop.first %}
      <div class="workshop-foundation-band" data-foundation="{{ f }}">
        {{ foundationInfo[f].emoji }} {{ foundationInfo[f].name }}
      </div>
      {% endif %}{% endfor %}{% endif %}
      {% if p.data.workshopDate %}<div style="background: var(--card); color: var(--accent); text-align: center; padding: 8px 14px 2px 14px; font-weight: 600; font-size: 1rem;">
        {{ p.data.workshopDate }}
      </div>{% endif %}
      {% if p.data.location %}<div style="background: var(--card); text-align: center; font-size: 0.9rem; color: var(--link); padding: 0 14px 8px 14px;">{{ p.data.location }}</div>{% endif %}
      <a href="{{ p.url }}" class="thumb" aria-label="{{ p.data.title }}">
        {% if p.data.image %}
          <img src="{{ p.data.image }}" alt="{{ p.data.title }}" />
        {% else %}
          [FIELD SCHOOL]
        {% endif %}
      </a>
      <div class="card-content">
        <h3 style="text-align: center;"><a href="{{ p.url }}">{{ p.data.title }}</a></h3>
        {% if p.data.meta %}<div class="meta-description">{{ p.data.meta }}</div>{% endif %}
        <div style="text-align: center; margin-top: auto; padding-top: 12px;">
          <a href="{{ p.url }}" style="display: inline-block; background: var(--accent); color: white; padding: 0.5rem 1rem; border-radius: 6px; text-decoration: none; font-weight: 600;">Register</a>
        </div>
      </div>
    </article>
    {% endif %}{% endfor %}
    </div>
  </div>
</section>

<section style="margin-bottom: 2rem;" id="recent-workshops">
  <div style="background: var(--card); border: 1px solid var(--hair); border-radius: 16px; padding: 1.5rem; box-shadow: var(--shadow);">
    <h2 style="color: var(--accent); font-size: 1.4rem; text-align: center; margin: 0 0 0.5rem;">Recent Workshops</h2>
    <p style="color: var(--muted); text-align: center; max-width: 600px; margin: 0 auto 1rem;">Here are some workshops we've held in the past, and may offer again soon. Click through to see photos and details!</p>
    <div class="grid">
    <article class="card">
      <div class="workshop-foundation-band" data-foundation="energy">
        {{ foundationInfo.energy.emoji }} {{ foundationInfo.energy.name }}
      </div>
      <div style="background: var(--card); color: var(--accent); text-align: center; padding: 8px 14px 2px 14px; font-weight: 600; font-size: 1rem;">
        October 2025
      </div>
      <div style="background: var(--card); text-align: center; font-size: 0.9rem; color: var(--link); padding: 0 14px 8px 14px;">Waltham, MA</div>
      <a href="/events/farmday/" class="thumb" aria-label="Waltham Fields Community Farm: Farm Day 2025">
        <img src="/assets/images/farmday/Farm_Day_2025_Square.jpg" alt="Waltham Fields Community Farm: Farm Day 2025" />
      </a>
      <div class="card-content">
        <h3 style="text-align: center;"><a href="/events/farmday/">Waltham Fields Community Farm: Farm Day 2025</a></h3>
        <div class="meta-description">Hands-on solar demos, hand drills, and DIY fun at Waltham Fields Community Farm</div>
      </div>
    </article>
    <article class="card">
      <div class="workshop-foundation-band" data-foundation="communication">
        {{ foundationInfo.communication.emoji }} {{ foundationInfo.communication.name }}
      </div>
      <div style="background: var(--card); color: var(--accent); text-align: center; padding: 8px 14px 2px 14px; font-weight: 600; font-size: 1rem;">
        July 2025
      </div>
      <div style="background: var(--card); text-align: center; font-size: 0.9rem; color: var(--link); padding: 0 14px 8px 14px;">Danville, VT</div>
      <a href="/workshops/diy-communicators/" class="thumb" aria-label="Off-Grid DIY Radio Communicators">
        <img src="/assets/images/diy-communicators/main.webp" alt="Off-Grid DIY Radio Communicators" />
      </a>
      <div class="card-content">
        <h3 style="text-align: center;"><a href="/workshops/diy-communicators/">Off-Grid DIY Radio Communicators</a></h3>
        <div class="meta-description">Hands-on workshop building mesh radio communicators at Otherwise Summer Camp</div>
      </div>
    </article>
    <article class="card">
      <div class="workshop-foundation-band" data-foundation="energy">
        {{ foundationInfo.energy.emoji }} {{ foundationInfo.energy.name }}
      </div>
      <div style="background: var(--card); color: var(--accent); text-align: center; padding: 8px 14px 2px 14px; font-weight: 600; font-size: 1rem;">
        October 2025
      </div>
      <div style="background: var(--card); text-align: center; font-size: 0.9rem; color: var(--link); padding: 0 14px 8px 14px;">Belmont, MA</div>
      <a href="/events/solar/" class="thumb" aria-label="Solar 101 at Beaver Brook">
        <img src="/assets/images/projects/solar101.jpg" alt="Solar 101 at Beaver Brook" />
      </a>
      <div class="card-content">
        <h3 style="text-align: center;"><a href="/events/solar/">Solar 101 at Beaver Brook</a></h3>
        <div class="meta-description">Introduction to off-grid solar power - experimenting with panels, batteries, and motors</div>
      </div>
    </article>
    </div>
  </div>
</section>

<section style="margin-bottom: 2rem;" id="future-workshops">
  <div style="background: var(--card); border: 1px solid var(--hair); border-radius: 16px; padding: 1.5rem; box-shadow: var(--shadow);">
    <h2 style="color: var(--accent); font-size: 1.4rem; text-align: center; margin: 0 0 0.5rem;">Future Workshops</h2>
    <p style="color: var(--muted); text-align: center; max-width: 600px; margin: 0 auto 1rem;">Below are some workshops we are considering offering soon. If you'd be interested to attend (or teach) any of them — or have ideas for other workshops we might offer — please reach out!</p>
    <p class="contact-email" style="text-align: center; margin: 0 auto 1rem;">
      <a href="mailto:info@waterbearfieldschool.org?subject=Workshop%20Inquiry">info@waterbearfieldschool.org</a>
    </p>
    <div class="grid">
    {% for p in collections.allWorkshops %}{% if not p.data.current %}
    <article class="card">
      {% if p.data.foundations %}{% for f in p.data.foundations %}{% if loop.first %}
      <div class="workshop-foundation-band" data-foundation="{{ f }}">
        {{ foundationInfo[f].emoji }} {{ foundationInfo[f].name }}
      </div>
      {% endif %}{% endfor %}{% endif %}
      <a href="{{ p.url }}" class="thumb" aria-label="{{ p.data.title }}">
        {% if p.data.image %}
          <img src="{{ p.data.image }}" alt="{{ p.data.title }}" />
        {% else %}
          [FIELD SCHOOL]
        {% endif %}
      </a>
      <div class="card-content">
        <h3 style="text-align: center;"><a href="{{ p.url }}">{{ p.data.title }}</a></h3>
        {% if p.data.meta %}<div class="meta-description">{{ p.data.meta }}</div>{% endif %}
      </div>
    </article>
    {% endif %}{% endfor %}
    </div>
  </div>
</section>

