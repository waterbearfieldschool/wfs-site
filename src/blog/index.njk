---
title: "Journal"
description: "News and updates from Waterbear Field School"
permalink: /journal/
---
{% extends "layouts/base.njk" %}
{% block content %}
<div class="prose" style="max-width: 800px; margin: 0 auto;">
  <h1>Journal</h1>
  <p class="meta" style="margin-bottom: 2rem;">News, updates, and stories from Waterbear Field School</p>

  <div class="grid" style="grid-template-columns: repeat(2, 1fr);">
    {% for post in collections.blog %}
    <article class="card" style="overflow: hidden;">
      {% if post.data.image %}
      <a href="{{ post.url }}" class="thumb" aria-label="{{ post.data.title }}">
        <img src="{{ post.data.image }}" alt="{{ post.data.title }}" />
      </a>
      {% endif %}
      <div class="card-content">
        <time datetime="{{ post.date.toISOString() }}" style="color: var(--muted); font-size: 0.85rem;">
          {{ post.date.toDateString() }}
        </time>
        <h2 style="margin: 0.5rem 0; font-size: 1.1rem;"><a href="{{ post.url }}">{{ post.data.title }}</a></h2>
        {% if post.data.excerpt %}
        <p style="color: var(--muted); margin: 0.5rem 0; font-size: 0.9rem;">{{ post.data.excerpt }}</p>
        {% endif %}
        <a href="{{ post.url }}" style="font-size: 0.9rem;">Read more →</a>
      </div>
    </article>
    {% endfor %}
  </div>
</div>
{% endblock %}
