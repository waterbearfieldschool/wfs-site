{% extends "layouts/base.njk" %}
{% block content %}
<h2 class="section-title">{{ heading }}</h2>
<div class="list">
  {% for item in items %}
  <div class="post"><time datetime="{{ item.date | date('yyyy-LL-dd') }}">{{ item.date | date('LLL d, yyyy') }}</time> — <a href="{{ item.url }}">{{ item.data.title }}</a></div>
  {% endfor %}
</div>
{% endblock %}