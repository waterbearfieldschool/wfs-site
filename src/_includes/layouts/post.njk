{% extends "layouts/base.njk" %}
{% block content %}
<article class="prose">
  <h1>{{ title }}</h1>
  {% if date and not hideDate %}<div class="meta">{{ date.toDateString() }}</div>{% endif %}
  {{ content | safe }}
</article>
{% endblock %}