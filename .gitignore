# Build output — regenerated by `npm run build`. Don't track.
_site/

# Node dependencies — regenerated by `npm install`. Don't track.
node_modules/

# OS / editor cruft
.DS_Store
*.swp
*~

# Local-only configs (per-host)
.env
.env.local
